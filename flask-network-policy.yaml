apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-ingress-to-flask
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: flask-api
  policyTypes:
  - Ingress
  ingress: []
